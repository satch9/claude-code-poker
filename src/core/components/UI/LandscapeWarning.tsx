import React from 'react';

// Mode portrait désormais supporté : on n'affiche plus l'overlay forçant le
// paysage. La table ovale est redimensionnée pour rentrer dans la largeur
// disponible (cf. useResponsiveClasses + contraintes de seats).
export const LandscapeWarning: React.FC = () => {
  return null;
};
